services:
  postgres:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: root
      POSTGRES_DB: s_index_local
    ports:
      - 43997:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
